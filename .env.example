# Postgres connection string.
# Local dev: run `npx prisma dev` and copy the TCP url it prints.
# Production (Vercel): use your Neon/Vercel Postgres connection string
# (prefer the pooled "-pooler" host for serverless).
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
